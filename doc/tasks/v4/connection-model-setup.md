# Connection Model And Setup

## Goal

Model Excel upload as a `static_file` connection and prepare the same concept
for future source integrations.

## Tasks

- [ ] Define connection data model.
- [ ] Create `static_file` connection setup flow.
- [ ] Store source metadata on the connection.
- [ ] Detect workbook sheets after upload.
- [ ] Let the user select sheets for dataset creation.

## Testing

- [ ] Add backend tests for connection creation.
- [ ] Add integration tests for workbook-to-connection setup.

