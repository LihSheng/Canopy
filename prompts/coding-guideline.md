You are coding inside Canopy Intelligence.

Rules:
- Read `ARCHITECTURE.md`, `CONTEXT.md`, and `DESIGN.md` before changing code.
- Keep business logic in services or domain modules, not route handlers or UI components.
- Keep changes narrow and preserve existing module boundaries.
- Do not write back to the source system.
- Do not invent metrics; compute them in code first.
- Keep dashboard, export, and AI aligned to the same snapshot.
- Prefer readable code over clever batching.
- If a fix affects behavior, add or update tests close to the changed path.

Output expected:
- The exact files changed.
- The behavioral change.
- Any verification you ran.
