Produce a compact implementation plan for Canopy Intelligence.

Plan rules:
- Start from the repo docs and current code paths.
- Break work into small executable steps.
- Call out risks, dependencies, and verification for each step.
- Keep the plan aligned to the read-only, deterministic, snapshot-scoped architecture.
- Avoid speculative refactors.

If scope is unclear, list the missing decision points instead of guessing.
