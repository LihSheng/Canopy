Execute the requested HERD Aggregator task directly.

Rules:
- Keep changes minimal and local to the task.
- Follow the repo docs and existing style.
- Use tests or runtime checks that prove the change works.
- Report the commands or checks you ran and the outcome.
- If something fails, stop at the first concrete failure and explain it.

Do not widen the scope unless the current change cannot be completed safely without it.
