# Domain Docs

This repository is a single-context project:

- `CONTEXT.md` is located at the repo root.
- Architectural Decision Records (`docs/adr/`) are located at the repo root.

All domain-specific terminology and architectural decisions are consolidated here.
