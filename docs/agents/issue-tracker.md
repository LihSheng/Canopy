# Issue Tracker

This repository uses local markdown files for issue tracking, located in `.scratch/`.

- To create an issue: Create a new file in `.scratch/` named `<issue-id>-<short-description>.md`.
- To update an issue: Modify the corresponding markdown file.
- The `triage` skill manages moving files between subdirectories corresponding to triage states.
